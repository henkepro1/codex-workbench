# Project Registry

This folder tracks project dossiers at the workbench level.

Use `index.json` as the compact registry of all project dossiers. Each project should keep detailed project context inside `projects/<slug>/.ai/`.
